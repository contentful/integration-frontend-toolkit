import tokens from '@contentful/f36-tokens';
import { css } from '@emotion/css';

export const styles = {
    root: css({
        position: 'relative',
    }),
    missingBadge: css({
        marginBottom: tokens.spacingXs
    })
};
